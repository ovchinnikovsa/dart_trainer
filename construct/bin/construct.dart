void main() {
  // IF ELSE
  /*
  var grade = 5;
  if (grade == 5) {
    print('Well done!');
  } else {
    print('Very bad!');
  }

  var marks = 60;
  if (marks >= 90 && marks <= 100) {
    print('Grade A+');
  } else if (marks >= 80 && marks <= 90) {
    print('Grade A');
  } else if (marks >= 60 && marks <= 80) {
    print('Grade B');
  } else {
    print('Invalide mark!');
  }

  // ? :

  int x = 1;
  int y = 2;
  if (x < y) {
    print(x + y);
  } else if (x > y) {
    print(x - y);
  }

  int z;
  z = x < y ? x + y : x - y;
  print(z);

// SWITCH
  String ball = 'B';
  switch (ball) {
    case 'A':
      print('too good');
      break;
    case 'B':
      print('all right');
      break;
    case 'C':
      print('damn');
      break;
    default:
      print('very bad');
  }

  // DO WHILE
  int counter = 0;
  do {
    if (counter == 2) {
      continue;
    }
    if (counter == 6) {
      break;
    }
    print('Hello Dart! $counter');
    counter++;
  } while (counter < 10);

  // WHILE
  int count = 0;
  while (count < 10) {
    print('Hello while $count');
    count++;
  }

  // FOR
  for (int i = 0; i < 10; i++) {
    if (i % 2 == 0) {
      print('Hello for $i');
    }
  }
  */
}
